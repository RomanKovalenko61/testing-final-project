# Testing Final Project

Проект автоматизированного тестирования для веб-сайта Selenium Academy и мобильного приложения Wikipedia с использованием Selenium WebDriver, Appium и TestNG.

## 📋 Описание проекта

Данный проект содержит автоматизированные тесты для:
- **Web-тестирования**: Тестирование сайта [Selenium Academy](https://selenium.academy) с использованием Selenium WebDriver
- **Mobile-тестирования**: Тестирование Android-приложения Wikipedia с использованием Appium

Проект реализован с использованием паттерна Page Object Model (POM) для удобства поддержки и расширения тестов.

### Структура проекта

```
testing-final-project/
├── src/
│   ├── main/java/                      # Основной код приложения
│   └── test/
│       ├── java/ru/mephi/
│       │   ├── pages/                  # Page Object классы
│       │   │   ├── mobile/             # Страницы для мобильных тестов
│       │   │   │   ├── BaseMobilePage.java
│       │   │   │   └── WikipediaMainPage.java
│       │   │   └── web/                # Страницы для веб-тестов
│       │   │       ├── SeleniumAcademyLoginPage.java
│       │   │       └── SeleniumAcademyMainPage.java
│       │   ├── tests/                  # Тестовые классы
│       │   │   ├── mobile/             # Мобильные тесты
│       │   │   │   ├── BaseMobileTest.java
│       │   │   │   └── WikipediaTest.java
│       │   │   └── web/                # Веб-тесты
│       │   │       ├── BaseWebTest.java
│       │   │       ├── SeleniumAcademyTests.java
│       │   │       ├── LoginTests.java
│       │   │       └── CoursesMenuTests.java
│       │   └── utils/                  # Утилиты
│       │       └── ConfigReader.java
│       └── resources/
│           └── config.properties       # Конфигурация тестов
├── testng.xml                          # Конфигурация для веб-тестов
├── testng-mobile.xml                   # Конфигурация для мобильных тестов
├── Makefile                            # Команды для запуска тестов
└── pom.xml                             # Maven конфигурация
```

## 🛠 Требуемое окружение

### Общие требования

- **JDK**: Java Development Kit 17 или выше
  ```bash
  java -version  # Должна быть версия 17+
  ```
- **Maven**: Apache Maven 3.6+ для сборки проекта
  ```bash
  mvn -version
  ```
- **Make** (опционально): для использования Makefile команд
  ```bash
  make --version
  ```

### Для веб-тестов

- **Браузер**: Google Chrome (последняя версия)
- **WebDriverManager**: Автоматически загружает необходимые драйверы (встроено в проект)
- **Selenium WebDriver**: 4.39.0 (указано в pom.xml)

### Для мобильных тестов

#### Необходимые инструменты:

1. **Node.js и npm** (для Appium)
   ```bash
   # Проверка установки
   node -v  # Рекомендуется v16+
   npm -v
   ```

2. **Appium Server** (версия 2.x)
   ```bash
   # Установка Appium глобально
   npm install -g appium
   
   # Проверка установки
   appium -v
   ```

3. **Appium UiAutomator2 Driver**
   ```bash
   # Установка драйвера для Android
   appium driver install uiautomator2
   
   # Проверка установленных драйверов
   appium driver list
   ```

4. **Android SDK** и **Android Studio**
   - Скачайте и установите [Android Studio](https://developer.android.com/studio)
   - Установите Android SDK (минимум API Level 16)
   - Настройте переменные окружения:
     ```bash
     export ANDROID_HOME=$HOME/Android/Sdk
     export PATH=$PATH:$ANDROID_HOME/platform-tools
     export PATH=$PATH:$ANDROID_HOME/tools
     export PATH=$PATH:$ANDROID_HOME/emulator
     ```

5. **Android Emulator** или физическое устройство
   - **Рекомендуемая конфигурация эмулятора:**
     - Device: Pixel 7
     - Android Version: 16+ (API Level 16+)
     - System Image: x86_64 (для лучшей производительности)
   
   - **Создание эмулятора через AVD Manager:**
     ```bash
     # Открыть AVD Manager
     android avdmanager create avd -n Pixel_7 -k "system-images;android-34;google_apis;x86_64" -d "pixel_7"
     ```
   
   - **Запуск эмулятора:**
     ```bash
     emulator -avd Pixel_7
     # или через Android Studio: Tools > Device Manager
     ```

6. **ADB (Android Debug Bridge)**
   ```bash
   # Проверка подключения устройств
   adb devices
   ```

7. **Wikipedia APK** (для мобильных тестов)
   - Приложение должно быть установлено на эмуляторе/устройстве
   - Package: `org.wikipedia`
   - Activity: `org.wikipedia.main.MainActivity`
   - Можно установить через Google Play Store на эмуляторе или скачать APK

## 📦 Установка и настройка

### 1. Клонирование проекта

```bash
git clone <repository-url>
cd testing-final-project
```

### 2. Установка зависимостей

```bash
mvn clean install
```

### 3. Настройка конфигурации

Отредактируйте файл `src/test/resources/config.properties` при необходимости:

```properties
# Web Testing Configuration
web.base.url=https://selenium.academy
web.browser=chrome

# Test Login Data
test.login.email=test@example.com
test.login.password=testpassword

# Mobile Testing Configuration
appium.server.url=http://127.0.0.1:4723
android.platform.name=Android
android.platform.version=16
android.device.name=Pixel_7
android.app.package=org.wikipedia
android.app.activity=org.wikipedia.main.MainActivity
android.automation.name=UiAutomator2

# App Reset Behavior
android.noReset=false
android.fullReset=false
```

## 🚀 Запуск тестов

### Веб-тесты

#### Вариант 1: Использование Make (рекомендуется)

```bash
# Запуск всех веб-тестов
make test-web
```

#### Вариант 2: Использование Maven напрямую

```bash
# Запуск веб-тестов через Maven
mvn clean test -Dsurefire.suiteXmlFiles=testng.xml
```

#### Вариант 3: Запуск через TestNG XML в IDE

1. Откройте проект в IntelliJ IDEA
2. Найдите файл `testng.xml`
3. Правой кнопкой мыши → Run 'testng.xml'

**Веб-тесты включают:**
- `SeleniumAcademyTests` - основные тесты функциональности сайта
- `LoginTests` - тесты авторизации
- `CoursesMenuTests` - тесты меню курсов

### Мобильные тесты

#### Подготовка к запуску:

1. **Запустите Appium Server**
   ```bash
   appium
   # Сервер должен запуститься на http://127.0.0.1:4723
   ```

2. **Запустите Android Emulator** (если используете эмулятор)
   ```bash
   emulator -avd Pixel_7
   # или через Android Studio
   ```

3. **Проверьте подключение устройства**
   ```bash
   adb devices
   # Должно показать подключенное устройство
   ```

4. **Убедитесь, что Wikipedia установлена**
   ```bash
   adb shell pm list packages | grep wikipedia
   # Должен вывести: package:org.wikipedia
   ```

#### Вариант 1: Использование Make (рекомендуется)

```bash
# Запуск всех мобильных тестов
make test-mobile
```

#### Вариант 2: Использование Maven напрямую

```bash
# Запуск мобильных тестов через Maven
mvn clean test -Dsurefire.suiteXmlFiles=testng-mobile.xml
```

#### Вариант 3: Запуск через TestNG XML в IDE

1. Откройте проект в IntelliJ IDEA
2. Найдите файл `testng-mobile.xml`
3. Правой кнопкой мыши → Run 'testng-mobile.xml'

**Мобильные тесты включают:**
- `WikipediaTest` - тесты функциональности приложения Wikipedia

### Дополнительные команды

```bash
# Показать все доступные команды
make help

# Очистить проект
make clean

# Собрать проект
make compile
```

## 📊 Отчеты о тестировании

После выполнения тестов отчеты генерируются в директории:

```
target/surefire-reports/
├── emailable-report.html    # HTML отчет (можно открыть в браузере)
├── index.html               # Главный индекс отчетов
├── testng-results.xml       # XML результаты для интеграций
└── junitreports/            # JUnit совместимые отчеты
```

**Просмотр отчетов:**
```bash
# Открыть HTML отчет в браузере
xdg-open target/surefire-reports/emailable-report.html
# или
firefox target/surefire-reports/emailable-report.html
```

## 🔧 Устранение неполадок

### Веб-тесты

**Проблема:** Драйвер браузера не найден
```bash
# Решение: WebDriverManager должен автоматически скачать драйвер
# Убедитесь, что есть подключение к интернету при первом запуске
```

**Проблема:** Браузер не запускается
```bash
# Проверьте, установлен ли Chrome
google-chrome --version

# Убедитесь, что версия Selenium совместима с версией браузера
```

### Мобильные тесты

**Проблема:** Appium Server не запускается
```bash
# Переустановите Appium
npm uninstall -g appium
npm install -g appium
appium driver install uiautomator2
```

**Проблема:** Устройство не обнаруживается
```bash
# Перезапустите ADB server
adb kill-server
adb start-server
adb devices
```

**Проблема:** Приложение не запускается на эмуляторе
```bash
# Проверьте, что приложение установлено
adb shell pm list packages | grep wikipedia

# Если нет, установите Wikipedia через Play Store или:
adb install path/to/wikipedia.apk
```

**Проблема:** Ошибка "Could not find a connected Android device"
```bash
# Убедитесь, что эмулятор запущен и полностью загрузился
adb devices

# Должен показать:
# List of devices attached
# emulator-5554   device
```

**Проблема:** Ошибка подключения к Appium
```bash
# Убедитесь, что Appium запущен на правильном порту
# По умолчанию: http://127.0.0.1:4723

# Проверьте config.properties:
appium.server.url=http://127.0.0.1:4723
```

## 📝 Конфигурация тестов

### Изменение браузера (веб-тесты)

В файле `config.properties`:
```properties
web.browser=chrome  # Доступно: chrome, firefox, edge
```

### Изменение устройства (мобильные тесты)

В файле `config.properties`:
```properties
android.device.name=Pixel_7        # Имя вашего AVD или устройства
android.platform.version=16        # Версия Android
```

## 🧪 Технологический стек

- **Java 17** - язык программирования
- **Maven 3.11+** - система сборки
- **TestNG 7.11.0** - фреймворк для тестирования
- **Selenium WebDriver 4.39.0** - автоматизация веб-браузеров
- **Appium Java Client 10.0.0** - автоматизация мобильных приложений
- **WebDriverManager 6.3.3** - управление драйверами браузеров
- **SLF4J 2.0.17** - логирование


